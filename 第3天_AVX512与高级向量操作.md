# 第3天：AVX-512与高级向量操作

## 课程目标
- 理解AVX-512指令集的新特性
- 掌握掩码操作（Mask Operations）
- 学习复数点积的SIMD实现
- 实现Cosine和L2距离函数
- 深入spatial.h模块

---

## 第一部分：AVX-512基础

### 1.1 AVX-512的革命性变化

**从AVX2到AVX-512的三大飞跃**：

```
1. 向量宽度翻倍
   AVX2:  256 bits (8× float32)
   AVX-512: 512 bits (16× float32)

2. 引入掩码寄存器（Mask Registers）
   - 8个专用掩码寄存器：k0-k7
   - 支持条件执行和向量长度灵活性

3. 更丰富的指令集
   - 嵌入式舍入控制
   - 更多的垂直和水平操作
   - 冲突检测、压缩、扩展等
```

### 1.2 AVX-512数据类型

```c
// 512位向量类型
__m512  vec_f32;   // 16× float32
__m512d vec_f64;   // 8× float64
__m512i vec_i32;   // 16× int32, 32× int16, 64× int8

// 掩码类型（每bit对应一个元素）
__mmask8  mask8;   // 8-bit掩码（用于float64）
__mmask16 mask16;  // 16-bit掩码（用于float32）
__mmask32 mask32;  // 32-bit掩码（用于int16）
__mmask64 mask64;  // 64-bit掩码（用于int8）
```

### 1.3 掩码操作（Masking） - AVX-512的核心

**掩码作用**：选择性地对向量元素执行操作

```c
// 传统方法（AVX2）：处理尾部元素需要额外的标量循环
for (size_t i = 0; i + 8 <= n; i += 8) {
    // SIMD处理
}
for (; i < n; ++i) {
    // 标量处理剩余元素
}

// AVX-512方法：使用掩码一次性处理所有元素（包括尾部）
size_t remaining = n % 16;
__mmask16 mask = (1 << remaining) - 1;  // 例如：remaining=5 -> mask=0b11111
__m512 a = _mm512_maskz_loadu_ps(mask, ptr);  // 只加载有效元素
```

**三种掩码模式**：

```c
// 1. Merge Masking（合并）：保留目标寄存器中被屏蔽的元素
__m512 result = _mm512_mask_add_ps(src, mask, a, b);
// 如果mask[i]=1: result[i] = a[i] + b[i]
// 如果mask[i]=0: result[i] = src[i]  (保留原值)

// 2. Zero Masking（清零）：被屏蔽的元素设为0
__m512 result = _mm512_maskz_add_ps(mask, a, b);
// 如果mask[i]=1: result[i] = a[i] + b[i]
// 如果mask[i]=0: result[i] = 0

// 3. 掩码加载/存储
__m512 a = _mm512_maskz_loadu_ps(mask, ptr);  // 只加载mask=1的元素
_mm512_mask_storeu_ps(ptr, mask, a);          // 只存储mask=1的元素
```

---

## 第二部分：实现AVX-512点积

### 2.1 查看SimSIMD的Skylake实现

```bash
# 查找Skylake后端的dot实现
grep -A 80 "simsimd_dot_f32_skylake" /home/dev/SimSIMD/include/simsimd/dot.h | head -100
```

### 2.2 完整的Skylake Dot Product实现

```c
SIMSIMD_PUBLIC void simsimd_dot_f32_skylake(
    simsimd_f32_t const* a,
    simsimd_f32_t const* b,
    simsimd_size_t n,
    simsimd_distance_t* result) {

    __m512 ab_vec = _mm512_setzero_ps();  // 16× float32累加器
    simsimd_size_t i = 0;

    // 主循环：每次处理16个元素
    for (; i + 16 <= n; i += 16) {
        __m512 a_vec = _mm512_loadu_ps(a + i);
        __m512 b_vec = _mm512_loadu_ps(b + i);
        ab_vec = _mm512_fmadd_ps(a_vec, b_vec, ab_vec);
    }

    // 尾部处理：使用掩码（AVX-512的杀手锏）
    if (i < n) {
        __mmask16 mask = (__mmask16)((1ULL << (n - i)) - 1);
        __m512 a_vec = _mm512_maskz_loadu_ps(mask, a + i);
        __m512 b_vec = _mm512_maskz_loadu_ps(mask, b + i);
        ab_vec = _mm512_mask_fmadd_ps(a_vec, mask, b_vec, ab_vec);
    }

    // 水平归约（512位 -> 标量）
    *result = _mm512_reduce_add_ps(ab_vec);
}
```

**关键改进**：
1. **无需标量尾部循环**：掩码处理所有情况
2. **内置归约指令**：`_mm512_reduce_add_ps`
3. **更大的向量宽度**：16个元素 vs AVX2的8个

### 2.3 AVX-512水平归约

```c
// AVX-512提供了更简单的归约方法
float sum = _mm512_reduce_add_ps(vec);

// 等价于手动实现：
SIMSIMD_INTERNAL simsimd_f32_t _simsimd_reduce_f32x16_skylake(__m512 vec) {
    // 方法1：使用内置函数（推荐）
    return _mm512_reduce_add_ps(vec);

    // 方法2：手动归约
    __m256 low = _mm512_castps512_ps256(vec);       // 低256位
    __m256 high = _mm512_extractf32x8_ps(vec, 1);   // 高256位
    __m256 sum256 = _mm256_add_ps(low, high);
    // 然后使用AVX2的归约方法...
    return _simsimd_reduce_f32x8_haswell(sum256);
}
```

---

## 第三部分：复数点积（Complex Dot Product）

### 3.1 复数运算基础

**复数乘法**：
```
(a + bi) × (c + di) = (ac - bd) + (ad + bc)i

实部：ac - bd
虚部：ad + bc
```

**两种复数点积**：

```c
// 1. dot (不共轭)
dot(A, B) = Σ(a[i] × b[i])
          = Σ((a_r + a_i·i) × (b_r + b_i·i))
          = Σ((a_r·b_r - a_i·b_i) + (a_r·b_i + a_i·b_r)i)

// 2. vdot (共轭点积)
vdot(A, B) = Σ(conj(a[i]) × b[i])
           = Σ((a_r - a_i·i) × (b_r + b_i·i))
           = Σ((a_r·b_r + a_i·b_i) + (a_r·b_i - a_i·b_r)i)
```

### 3.2 复数SIMD实现策略

**数据布局**：
```c
// 交错存储（Interleaved）：{r0, i0, r1, i1, r2, i2, ...}
float complex_array[8] = {
    a0_real, a0_imag,  // 元素0
    a1_real, a1_imag,  // 元素1
    a2_real, a2_imag,  // 元素2
    a3_real, a3_imag   // 元素3
};

// AVX2向量可以同时处理4个复数（8个float）
```

### 3.3 查看SimSIMD的复数实现

```bash
# 查找复数dot实现
grep -A 100 "simsimd_dot_f32c_haswell" /home/dev/SimSIMD/include/simsimd/dot.h | head -120
```

### 3.4 复数点积的SIMD实现（AVX2）

```c
SIMSIMD_PUBLIC void simsimd_dot_f32c_haswell(
    simsimd_f32c_t const* a,  // 复数数组
    simsimd_f32c_t const* b,
    simsimd_size_t n,
    simsimd_distance_t* results) {  // 输出：{real, imag}

    __m256 ab_real_vec = _mm256_setzero_ps();  // 实部累加器
    __m256 ab_imag_vec = _mm256_setzero_ps();  // 虚部累加器

    simsimd_size_t i = 0;
    for (; i + 4 <= n; i += 4) {  // 每次4个复数 = 8个float
        // 加载数据：{a0_r, a0_i, a1_r, a1_i, a2_r, a2_i, a3_r, a3_i}
        __m256 a_vec = _mm256_loadu_ps((float const*)(a + i));
        __m256 b_vec = _mm256_loadu_ps((float const*)(b + i));

        // 分离实部和虚部
        // a_real: {a0_r, a0_r, a1_r, a1_r, a2_r, a2_r, a3_r, a3_r}
        __m256 a_real = _mm256_moveldup_ps(a_vec);
        // a_imag: {a0_i, a0_i, a1_i, a1_i, a2_i, a2_i, a3_i, a3_i}
        __m256 a_imag = _mm256_movehdup_ps(a_vec);

        // 交换b的实部虚部：{b0_i, b0_r, b1_i, b1_r, ...}
        __m256 b_swapped = _mm256_permute_ps(b_vec, 0xB1);

        // 计算实部：a_real * b_vec = {a0_r*b0_r, a0_r*b0_i, ...}
        __m256 prod_real = _mm256_mul_ps(a_real, b_vec);

        // 计算虚部：a_imag * b_swapped = {a0_i*b0_i, a0_i*b0_r, ...}
        __m256 prod_imag = _mm256_mul_ps(a_imag, b_swapped);

        // 实部累加（需要减去虚部的交叉项）
        ab_real_vec = _mm256_fmadd_ps(a_real, b_vec, ab_real_vec);
        ab_real_vec = _mm256_fmsub_ps(a_imag, b_swapped, ab_real_vec);

        // 虚部累加
        ab_imag_vec = _mm256_fmadd_ps(a_real, b_swapped, ab_imag_vec);
        ab_imag_vec = _mm256_fmadd_ps(a_imag, b_vec, ab_imag_vec);
    }

    // 归约
    simsimd_f32_t ab_real = _simsimd_reduce_f32x8_haswell(ab_real_vec);
    simsimd_f32_t ab_imag = _simsimd_reduce_f32x8_haswell(ab_imag_vec);

    // 尾部处理...

    results[0] = ab_real;
    results[1] = ab_imag;
}
```

**关键技巧**：
- `_mm256_moveldup_ps`：复制低位（实部）
- `_mm256_movehdup_ps`：复制高位（虚部）
- `_mm256_permute_ps`：交换元素顺序

---

## 第四部分：空间距离函数 - Cosine & L2

### 4.1 Cosine距离（余弦距离）

**数学定义**：
```
cosine_similarity = (A·B) / (||A|| × ||B||)
cosine_distance = 1 - cosine_similarity
```

**标量实现**：
```c
float cosine_distance(float const* a, float const* b, size_t n) {
    float ab = 0, a2 = 0, b2 = 0;

    for (size_t i = 0; i < n; ++i) {
        ab += a[i] * b[i];
        a2 += a[i] * a[i];
        b2 += b[i] * b[i];
    }

    // 避免除零
    if (a2 == 0 || b2 == 0) return 1.0f;

    float similarity = ab / (sqrtf(a2) * sqrtf(b2));
    return 1.0f - similarity;
}
```

### 4.2 查看SimSIMD的Cosine实现

```bash
# 查看spatial.h中的cosine实现
grep -A 100 "simsimd_cos_f32_haswell" /home/dev/SimSIMD/include/simsimd/spatial.h | head -120
```

### 4.3 优化的Cosine SIMD实现

```c
SIMSIMD_PUBLIC void simsimd_cos_f32_haswell(
    simsimd_f32_t const* a,
    simsimd_f32_t const* b,
    simsimd_size_t n,
    simsimd_distance_t* result) {

    // 三个累加器：ab, a², b²
    __m256 ab_vec = _mm256_setzero_ps();
    __m256 a2_vec = _mm256_setzero_ps();
    __m256 b2_vec = _mm256_setzero_ps();

    simsimd_size_t i = 0;
    for (; i + 8 <= n; i += 8) {
        __m256 a_vec = _mm256_loadu_ps(a + i);
        __m256 b_vec = _mm256_loadu_ps(b + i);

        // 同时计算三个值（关键优化！）
        ab_vec = _mm256_fmadd_ps(a_vec, b_vec, ab_vec);
        a2_vec = _mm256_fmadd_ps(a_vec, a_vec, a2_vec);
        b2_vec = _mm256_fmadd_ps(b_vec, b_vec, b2_vec);
    }

    // 归约
    simsimd_f32_t ab = _simsimd_reduce_f32x8_haswell(ab_vec);
    simsimd_f32_t a2 = _simsimd_reduce_f32x8_haswell(a2_vec);
    simsimd_f32_t b2 = _simsimd_reduce_f32x8_haswell(b2_vec);

    // 尾部处理
    for (; i < n; ++i) {
        ab += a[i] * b[i];
        a2 += a[i] * a[i];
        b2 += b[i] * b[i];
    }

    // 计算距离
    simsimd_f32_t a2b2 = a2 * b2;
    if (a2b2 == 0) {
        *result = 1.0;
        return;
    }

    // 使用倒数平方根（rsqrt）代替 1/sqrt
    // rsqrt更快但精度稍低
    simsimd_f32_t rsqrt_a2b2 = 1.0f / sqrtf(a2b2);
    // 或使用AVX内置指令：
    // __m256 rsqrt_vec = _mm256_rsqrt_ps(_mm256_set1_ps(a2b2));
    // simsimd_f32_t rsqrt_a2b2 = _mm256_cvtss_f32(rsqrt_vec);

    *result = 1.0f - ab * rsqrt_a2b2;
}
```

**优化技巧**：
1. **融合计算**：一次循环计算ab、a²、b²
2. **rsqrt优化**：使用倒数平方根减少除法
3. **Newton-Raphson迭代**：提高rsqrt精度（可选）

### 4.4 牛顿-拉夫森迭代优化

SimSIMD使用N-R迭代提高rsqrt精度：

```c
// 硬件rsqrt（低精度，快速）
__m256 rsqrt = _mm256_rsqrt_ps(x);  // 相对误差 ~1.5×2^(-12)

// 一次Newton-Raphson迭代（精度提升~100倍）
// 公式：x_{n+1} = x_n * (3 - x * x_n^2) / 2
__m256 half = _mm256_set1_ps(0.5f);
__m256 three = _mm256_set1_ps(3.0f);
__m256 rsqrt_better = _mm256_mul_ps(
    rsqrt,
    _mm256_mul_ps(
        half,
        _mm256_fmsub_ps(three, rsqrt, _mm256_mul_ps(x, _mm256_mul_ps(rsqrt, rsqrt)))
    )
);  // 相对误差 ~2^(-23) - 接近float32极限
```

### 4.5 L2距离（欧几里得距离）

**数学定义**：
```
L2_distance = sqrt(Σ(a[i] - b[i])²)
L2_sq_distance = Σ(a[i] - b[i])²  (平方距离，更常用)
```

**SIMD实现**：
```c
SIMSIMD_PUBLIC void simsimd_l2sq_f32_haswell(
    simsimd_f32_t const* a,
    simsimd_f32_t const* b,
    simsimd_size_t n,
    simsimd_distance_t* result) {

    __m256 d2_vec = _mm256_setzero_ps();

    simsimd_size_t i = 0;
    for (; i + 8 <= n; i += 8) {
        __m256 a_vec = _mm256_loadu_ps(a + i);
        __m256 b_vec = _mm256_loadu_ps(b + i);

        // 计算差值
        __m256 diff = _mm256_sub_ps(a_vec, b_vec);

        // 平方并累加
        d2_vec = _mm256_fmadd_ps(diff, diff, d2_vec);
    }

    // 归约
    simsimd_f32_t d2 = _simsimd_reduce_f32x8_haswell(d2_vec);

    // 尾部
    for (; i < n; ++i) {
        simsimd_f32_t diff = a[i] - b[i];
        d2 += diff * diff;
    }

    *result = d2;
}
```

---

## 第五部分：性能对比与实战

### 5.1 创建综合测试程序

```c
#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <time.h>
#include <immintrin.h>

#define N 1536

// ... (包含上面的实现)

int main() {
    float* a = aligned_alloc(64, N * sizeof(float));
    float* b = aligned_alloc(64, N * sizeof(float));

    // 初始化
    for (size_t i = 0; i < N; ++i) {
        a[i] = (float)i / N;
        b[i] = (float)(N - i) / N;
    }

    printf("=== 点积测试 ===\n");
    float dot_s = dot_serial(a, b, N);
    float dot_a = dot_avx2(a, b, N);
    printf("Serial: %.6f\n", dot_s);
    printf("AVX2:   %.6f\n", dot_a);
    printf("误差:   %.6e\n\n", fabsf(dot_s - dot_a));

    printf("=== 余弦距离测试 ===\n");
    float cos_s = cosine_distance_serial(a, b, N);
    float cos_a = cosine_distance_avx2(a, b, N);
    printf("Serial: %.6f\n", cos_s);
    printf("AVX2:   %.6f\n", cos_a);
    printf("误差:   %.6e\n\n", fabsf(cos_s - cos_a));

    printf("=== L2平方距离测试 ===\n");
    float l2_s = l2sq_distance_serial(a, b, N);
    float l2_a = l2sq_distance_avx2(a, b, N);
    printf("Serial: %.6f\n", l2_s);
    printf("AVX2:   %.6f\n", l2_a);
    printf("误差:   %.6e\n\n", fabsf(l2_s - l2_a));

    free(a);
    free(b);
    return 0;
}
```

### 5.2 编译并运行

```bash
# Haswell (AVX2)
gcc -O3 -march=haswell -o test_spatial test_spatial.c -lm
./test_spatial

# Skylake (AVX-512)
gcc -O3 -march=skylake-avx512 -o test_spatial_512 test_spatial.c -lm
./test_spatial_512
```

---

## 第六部分：今日总结与作业

### 今日学习要点

1. ✅ **AVX-512掩码**：灵活处理任意长度向量
2. ✅ **复数SIMD**：交错存储与shuffle操作
3. ✅ **Cosine优化**：融合计算、rsqrt、N-R迭代
4. ✅ **L2距离**：简单但高效的实现

### 作业

#### 作业1：掩码练习
编写一个函数，使用AVX-512掩码处理长度不是16倍数的数组。

#### 作业2：复数点积
实现并测试complex float32的dot和vdot（共轭）。

#### 作业3：精度分析
对比使用和不使用Newton-Raphson迭代的cosine距离精度。

#### 作业4：阅读源码
深入研究spatial.h：
```bash
# 查看所有spatial函数
grep "SIMSIMD_PUBLIC void simsimd_" /home/dev/SimSIMD/include/simsimd/spatial.h | head -30
```

### 预习第4天内容

明天我们将学习：
- **ARM NEON编程**
- **SVE（可变长度向量）**
- **跨平台设计模式**

---

**下一课**：[第4天：ARM SIMD编程 - NEON与SVE](./第4天_ARM_SIMD编程.md)
