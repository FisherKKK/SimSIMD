# 第6天：概率分布与高级度量

## 课程目标
- 理解概率分布的相似度度量（KL散度、JS散度）
- 掌握曲线空间中的距离计算（Mahalanobis、Bilinear）
- 学习Element-wise操作（加权和、FMA）
- 理解对数函数的SIMD近似实现
- 深入probability.h、curved.h、elementwise.h模块

---

## 第一部分：概率分布距离

### 1.1 为什么需要特殊的距离度量？

**场景**：两个概率分布P和Q

```
P = [0.1, 0.2, 0.3, 0.4]  (归一化：Σp = 1.0)
Q = [0.2, 0.1, 0.4, 0.3]

问题：如何度量它们的"差异"？

❌ 欧几里得距离（L2）：√Σ(pi - qi)²
   - 忽略了概率分布的特殊性质
   - 不考虑分布的信息论意义

✅ KL散度：Σ pi × log(pi / qi)
   - 考虑概率分布的信息熵
   - 衡量使用Q近似P的信息损失
```

### 1.2 Kullback-Leibler散度（KL Divergence）

**定义**：
```
KL(P || Q) = Σ P(i) × log(P(i) / Q(i))
           = Σ P(i) × log(P(i)) - Σ P(i) × log(Q(i))
```

**性质**：
```
1. 非对称：KL(P||Q) ≠ KL(Q||P)
2. 非负：KL(P||Q) ≥ 0
3. KL(P||P) = 0
4. 不满足三角不等式（不是真正的距离）
```

**应用场景**：
```
1. 机器学习：
   - 变分自编码器（VAE）的损失函数
   - 知识蒸馏（Knowledge Distillation）
   - 策略梯度（Policy Gradient）

2. 信息论：
   - 编码效率分析
   - 信道容量计算

3. 统计学：
   - 模型选择
   - 假设检验
```

### 1.3 Jensen-Shannon散度（JS Divergence）

**定义**：
```
JS(P || Q) = √(KL(P || M)/2 + KL(Q || M)/2)

其中 M = (P + Q) / 2  (P和Q的平均分布)
```

**性质**：
```
1. 对称：JS(P||Q) = JS(Q||P)  ✅
2. 有界：0 ≤ JS(P||Q) ≤ 1
3. 满足三角不等式（是真正的度量）
4. 数值稳定性更好
```

**为什么JS更受欢迎？**
```
KL散度的问题：
- 如果Q(i) = 0 但 P(i) > 0，则 KL → ∞
- 数值不稳定

JS散度的优势：
- M = (P + Q) / 2 保证 M(i) > 0
- 对称性使其成为真正的距离
- GAN训练常用JS散度
```

### 1.4 标量实现

```c
// KL散度
float kl_divergence_serial(float const* p, float const* q, size_t n) {
    float epsilon = 1e-7f;  // 防止log(0)
    float kl = 0.0f;

    for (size_t i = 0; i < n; ++i) {
        float pi = p[i];
        float qi = q[i];

        // 跳过pi=0的情况（0 * log(0/q) = 0）
        if (pi > epsilon) {
            kl += pi * logf((pi + epsilon) / (qi + epsilon));
        }
    }

    return kl;
}

// JS散度
float js_divergence_serial(float const* p, float const* q, size_t n) {
    float epsilon = 1e-7f;
    float js = 0.0f;

    for (size_t i = 0; i < n; ++i) {
        float pi = p[i];
        float qi = q[i];
        float mi = (pi + qi) / 2.0f;

        // KL(P||M)的贡献
        if (pi > epsilon) {
            js += pi * logf((pi + epsilon) / (mi + epsilon));
        }

        // KL(Q||M)的贡献
        if (qi > epsilon) {
            js += qi * logf((qi + epsilon) / (mi + epsilon));
        }
    }

    return sqrtf(js / 2.0f);
}
```

**关键细节**：
1. **Epsilon项**：防止除零和log(0)
2. **条件判断**：跳过零概率项
3. **对数底数**：通常使用自然对数（ln）

---

## 第二部分：对数函数的SIMD实现

### 2.1 挑战：SIMD中没有log指令

```
问题：x86和ARM都没有原生的log向量指令

解决方案：
1. 标量循环：慢，失去SIMD优势
2. 查表法：精度低，内存占用大
3. 多项式近似：平衡速度和精度 ✅
```

### 2.2 对数近似原理

**IEEE 754浮点表示**：
```
float = (-1)^sign × 2^exponent × mantissa

log2(x) ≈ exponent + log2(mantissa)

其中 mantissa ∈ [1, 2)，可以用多项式近似 log2(mantissa)
```

**多项式近似（Horner方法）**：
```
log2(m) ≈ c0 + c1*m + c2*m² + c3*m³ + c4*m⁴ + c5*m⁵

系数通过最小化误差拟合得到：
c0 = -3.3241990
c1 =  3.1157899
c2 = -1.2315303
c3 =  2.5988452
c4 = -3.4436006e-2
c5 =  3.1821337e-1
```

### 2.3 查看SimSIMD的log2实现

```bash
# 查看NEON的log2实现
grep -A 30 "_simsimd_log2_f32_neon" /home/dev/SimSIMD/include/simsimd/probability.h
```

### 2.4 NEON log2实现详解

```c
SIMSIMD_PUBLIC float32x4_t _simsimd_log2_f32_neon(float32x4_t x) {
    // 阶段1：提取指数
    // float的bit表示：[sign(1)][exponent(8)][mantissa(23)]
    int32x4_t i = vreinterpretq_s32_f32(x);  // 位重新解释为整数

    // 提取指数：右移23位，减去偏移127
    int32x4_t e = vsubq_s32(
        vshrq_n_s32(vandq_s32(i, vdupq_n_s32(0x7F800000)), 23),  // (i & 0x7F800000) >> 23
        vdupq_n_s32(127)  // 减去偏移
    );
    float32x4_t e_float = vcvtq_f32_s32(e);  // 转换为float

    // 阶段2：提取尾数并归一化到[1, 2)
    // 保留尾数位，指数设为127（对应2^0=1）
    float32x4_t m = vreinterpretq_f32_s32(
        vorrq_s32(
            vandq_s32(i, vdupq_n_s32(0x007FFFFF)),  // 保留尾数
            vdupq_n_s32(0x3F800000)  // 指数=127 → 2^0=1
        )
    );

    // 阶段3：多项式逼近 log2(m)
    float32x4_t one = vdupq_n_f32(1.0f);
    float32x4_t p = vdupq_n_f32(-3.4436006e-2f);

    // Horner方法：从最高次项开始
    p = vmlaq_f32(vdupq_n_f32(3.1821337e-1f), m, p);  // p = 0.318... + m*p
    p = vmlaq_f32(vdupq_n_f32(-1.2315303f), m, p);
    p = vmlaq_f32(vdupq_n_f32(2.5988452f), m, p);
    p = vmlaq_f32(vdupq_n_f32(-3.3241990f), m, p);
    p = vmlaq_f32(vdupq_n_f32(3.1157899f), m, p);

    // 阶段4：组合结果
    // log2(x) = exponent + log2(mantissa)
    float32x4_t result = vaddq_f32(
        vmulq_f32(p, vsubq_f32(m, one)),  // p * (m - 1)
        e_float
    );

    return result;
}
```

**精度分析**：
```
相对误差：~2^(-16) ≈ 1.5 × 10^-5
绝对误差：< 0.001（在大部分范围）

vs 标准库 logf()：
- 精度：相对误差 ~2^(-23) ≈ 1.2 × 10^-7（更准确）
- 速度：SIMD版本快 3-5x
```

### 2.5 转换为自然对数

```c
// log2 转 ln (自然对数)
float ln(float x) {
    return log2(x) * 0.693147181f;  // ln(2)
}

// SIMD版本
float32x4_t log_neon(float32x4_t x) {
    float32x4_t log2_x = _simsimd_log2_f32_neon(x);
    return vmulq_f32(log2_x, vdupq_n_f32(0.693147181f));
}
```

---

## 第三部分：SIMD加速的KL和JS散度

### 3.1 NEON KL散度实现

```c
SIMSIMD_PUBLIC void simsimd_kl_f32_neon(
    simsimd_f32_t const* a,
    simsimd_f32_t const* b,
    simsimd_size_t n,
    simsimd_distance_t* result) {

    simsimd_f32_t epsilon = 1e-7f;
    float32x4_t epsilon_vec = vdupq_n_f32(epsilon);
    float32x4_t sum_vec = vdupq_n_f32(0);

    simsimd_size_t i = 0;

    // 主循环：每次处理4个元素
    for (; i + 4 <= n; i += 4) {
        // 加载数据
        float32x4_t a_vec = vld1q_f32(a + i);
        float32x4_t b_vec = vld1q_f32(b + i);

        // 计算比值：(a + ε) / (b + ε)
        float32x4_t ratio_vec = vdivq_f32(
            vaddq_f32(a_vec, epsilon_vec),
            vaddq_f32(b_vec, epsilon_vec)
        );

        // 计算log2(ratio)
        float32x4_t log_ratio_vec = _simsimd_log2_f32_neon(ratio_vec);

        // 乘以a并累加：sum += a * log2(ratio)
        float32x4_t prod_vec = vmulq_f32(a_vec, log_ratio_vec);
        sum_vec = vaddq_f32(sum_vec, prod_vec);
    }

    // 尾部处理
    float sum = vaddvq_f32(sum_vec);
    for (; i < n; ++i) {
        float ai = a[i];
        float bi = b[i];
        if (ai > epsilon) {
            sum += ai * log2f((ai + epsilon) / (bi + epsilon));
        }
    }

    // 转换为自然对数
    sum *= 0.693147181f;  // ln(2)

    *result = sum;
}
```

### 3.2 NEON JS散度实现

```c
SIMSIMD_PUBLIC void simsimd_js_f32_neon(
    simsimd_f32_t const* a,
    simsimd_f32_t const* b,
    simsimd_size_t n,
    simsimd_distance_t* result) {

    simsimd_f32_t epsilon = 1e-7f;
    float32x4_t epsilon_vec = vdupq_n_f32(epsilon);
    float32x4_t sum_vec = vdupq_n_f32(0);
    float32x4_t half = vdupq_n_f32(0.5f);

    simsimd_size_t i = 0;

    for (; i + 4 <= n; i += 4) {
        float32x4_t a_vec = vld1q_f32(a + i);
        float32x4_t b_vec = vld1q_f32(b + i);

        // 计算中间分布 M = (P + Q) / 2
        float32x4_t m_vec = vmulq_f32(
            vaddq_f32(a_vec, b_vec),
            half
        );

        // KL(P||M)的贡献
        float32x4_t ratio_a = vdivq_f32(
            vaddq_f32(a_vec, epsilon_vec),
            vaddq_f32(m_vec, epsilon_vec)
        );
        float32x4_t log_ratio_a = _simsimd_log2_f32_neon(ratio_a);
        float32x4_t contrib_a = vmulq_f32(a_vec, log_ratio_a);

        // KL(Q||M)的贡献
        float32x4_t ratio_b = vdivq_f32(
            vaddq_f32(b_vec, epsilon_vec),
            vaddq_f32(m_vec, epsilon_vec)
        );
        float32x4_t log_ratio_b = _simsimd_log2_f32_neon(ratio_b);
        float32x4_t contrib_b = vmulq_f32(b_vec, log_ratio_b);

        // 累加两项
        sum_vec = vaddq_f32(sum_vec, vaddq_f32(contrib_a, contrib_b));
    }

    // 归约和尾部处理
    float sum = vaddvq_f32(sum_vec);

    for (; i < n; ++i) {
        float ai = a[i];
        float bi = b[i];
        float mi = (ai + bi) / 2.0f;

        if (ai > epsilon) {
            sum += ai * log2f((ai + epsilon) / (mi + epsilon));
        }
        if (bi > epsilon) {
            sum += bi * log2f((bi + epsilon) / (mi + epsilon));
        }
    }

    // 转换并开方
    sum *= 0.693147181f / 2.0f;  // ln(2) / 2
    *result = sqrtf(sum);
}
```

### 3.3 AVX-512实现的优势

```c
// Skylake: 16个元素并行
SIMSIMD_PUBLIC void simsimd_kl_f32_skylake(
    simsimd_f32_t const* a,
    simsimd_f32_t const* b,
    simsimd_size_t n,
    simsimd_distance_t* result) {

    __m512 epsilon_vec = _mm512_set1_ps(1e-7f);
    __m512 sum_vec = _mm512_setzero_ps();

    simsimd_size_t i = 0;

    // 主循环
    for (; i + 16 <= n; i += 16) {
        __m512 a_vec = _mm512_loadu_ps(a + i);
        __m512 b_vec = _mm512_loadu_ps(b + i);

        // 使用AVX-512内置的对数近似（如果可用）
        // 或调用自定义实现
        __m512 ratio = _mm512_div_ps(
            _mm512_add_ps(a_vec, epsilon_vec),
            _mm512_add_ps(b_vec, epsilon_vec)
        );

        __m512 log_ratio = _simsimd_log_f32_skylake(ratio);
        __m512 prod = _mm512_mul_ps(a_vec, log_ratio);
        sum_vec = _mm512_add_ps(sum_vec, prod);
    }

    // 尾部：使用掩码
    if (i < n) {
        __mmask16 mask = (__mmask16)((1ULL << (n - i)) - 1);
        __m512 a_vec = _mm512_maskz_loadu_ps(mask, a + i);
        __m512 b_vec = _mm512_maskz_loadu_ps(mask, b + i);

        __m512 ratio = _mm512_div_ps(
            _mm512_add_ps(a_vec, epsilon_vec),
            _mm512_add_ps(b_vec, epsilon_vec)
        );
        __m512 log_ratio = _simsimd_log_f32_skylake(ratio);
        __m512 prod = _mm512_mul_ps(a_vec, log_ratio);
        sum_vec = _mm512_mask_add_ps(sum_vec, mask, sum_vec, prod);
    }

    float sum = _mm512_reduce_add_ps(sum_vec);
    sum *= 0.693147181f;

    *result = sum;
}
```

---

## 第四部分：曲线空间度量 - Curved.h

### 4.1 为什么需要"曲线"度量？

**欧几里得空间 vs 曲线空间**：

```
欧几里得空间（平坦）：
- 距离：d = ||x - y||
- 假设：各维度同等重要、独立

曲线空间（弯曲）：
- 距离：d = √((x-y)ᵀ M (x-y))
- M是度量矩阵，考虑维度之间的协方差
```

**实际应用**：
```
1. 高斯分布的距离：Mahalanobis距离
2. 二次型优化
3. 流形学习（Manifold Learning）
4. 量子计算中的保真度
```

### 4.2 Mahalanobis距离

**定义**：
```
d_M(x, y) = √((x - y)ᵀ Σ⁻¹ (x - y))

其中 Σ 是协方差矩阵
```

**标量实现**：
```c
// Mahalanobis距离
float mahalanobis_serial(
    float const* a,         // 向量1: n维
    float const* b,         // 向量2: n维
    float const* inv_cov,   // 协方差逆矩阵: n×n
    size_t n) {

    float sum = 0.0f;

    // 计算 (a - b)ᵀ Σ⁻¹ (a - b)
    for (size_t i = 0; i < n; ++i) {
        float diff_i = a[i] - b[i];

        // 计算 (Σ⁻¹ · (a-b))[i]
        float weighted_sum = 0.0f;
        for (size_t j = 0; j < n; ++j) {
            float diff_j = a[j] - b[j];
            float cov_ij = inv_cov[i * n + j];  // 行优先存储
            weighted_sum += cov_ij * diff_j;
        }

        sum += diff_i * weighted_sum;
    }

    return sqrtf(sum);
}
```

**矩阵视角**：
```
令 d = a - b  (差向量)
令 C = Σ⁻¹   (协方差逆矩阵)

Mahalanobis = √(dᵀ C d)
            = √(Σᵢ Σⱼ d[i] × C[i,j] × d[j])
```

### 4.3 双线性形式（Bilinear Form）

**定义**：
```
B(a, b, C) = aᵀ C b
           = Σᵢ Σⱼ a[i] × C[i,j] × b[j]
```

**与Mahalanobis的关系**：
```
Mahalanobis(a, b, C) = √B(a-b, a-b, C)
```

**标量实现**：
```c
// 双线性形式
float bilinear_serial(
    float const* a,    // 向量1: n维
    float const* b,    // 向量2: n维
    float const* c,    // 矩阵C: n×n
    size_t n) {

    float sum = 0.0f;

    // 方法1：朴素O(n²)
    for (size_t i = 0; i < n; ++i) {
        for (size_t j = 0; j < n; ++j) {
            sum += a[i] * c[i * n + j] * b[j];
        }
    }

    return sum;
}
```

### 4.4 优化：结合律改变计算顺序

**关键洞察**：
```
aᵀ C b = aᵀ (C b)  或  (aᵀ C) b

选择：先计算 C·b，再与a点积
- 外层循环：遍历a的每个元素 a[i]
- 内层循环：计算 (C·b)[i] = Σⱼ C[i,j] × b[j]
- 每个C[i,j]只加载一次！
```

**优化实现**：
```c
float bilinear_optimized(
    float const* a,
    float const* b,
    float const* c,
    size_t n) {

    float sum = 0.0f;

    for (size_t i = 0; i < n; ++i) {
        // 计算 (C·b)[i]
        float cb_i = 0.0f;
        for (size_t j = 0; j < n; ++j) {
            cb_i += c[i * n + j] * b[j];
        }

        // 累加 a[i] × (C·b)[i]
        sum += a[i] * cb_i;
    }

    return sum;
}
```

**复杂度分析**：
```
朴素方法：O(n²) 乘法 + O(n²) 内存访问
优化方法：O(n²) 乘法 + O(n²) 内存访问（一样！）

但是：
- 优化方法的内存访问模式更规律
- 更利于SIMD向量化
- 每个矩阵元素只加载一次
```

### 4.5 SIMD优化的Bilinear Form

```c
SIMSIMD_PUBLIC void simsimd_bilinear_f32_haswell(
    simsimd_f32_t const* a,
    simsimd_f32_t const* b,
    simsimd_f32_t const* c,
    simsimd_size_t n,
    simsimd_distance_t* result) {

    __m256 sum_vec = _mm256_setzero_ps();

    for (simsimd_size_t i = 0; i < n; ++i) {
        // 广播 a[i] 到所有通道
        __m256 a_i = _mm256_set1_ps(a[i]);

        // 计算 (C·b)[i]，内层循环向量化
        __m256 cb_i_vec = _mm256_setzero_ps();

        simsimd_size_t j = 0;
        for (; j + 8 <= n; j += 8) {
            __m256 b_vec = _mm256_loadu_ps(b + j);
            __m256 c_vec = _mm256_loadu_ps(c + i * n + j);
            cb_i_vec = _mm256_fmadd_ps(c_vec, b_vec, cb_i_vec);
        }

        // 归约内层和
        float cb_i = _simsimd_reduce_f32x8_haswell(cb_i_vec);

        // 处理尾部
        for (; j < n; ++j) {
            cb_i += c[i * n + j] * b[j];
        }

        // 累加到总和：sum += a[i] * cb[i]
        sum_vec = _mm256_fmadd_ps(
            a_i,
            _mm256_set1_ps(cb_i),
            sum_vec
        );
    }

    *result = _simsimd_reduce_f32x8_haswell(sum_vec);
}
```

**关键优化**：
1. **内层循环向量化**：计算C·b时使用SIMD
2. **广播a[i]**：避免重复加载
3. **FMA指令**：减少指令数

---

## 第五部分：Element-wise操作 - Elementwise.h

### 5.1 基础操作

**Weighted Sum (加权和)**：
```
WSum(A, B, α, β) = α·A + β·B

特殊情况：
- 向量加法：α=β=1  → A + B
- 向量缩放：β=0     → α·A
- 向量平均：α=β=0.5 → (A + B) / 2
```

**FMA (Fused Multiply-Add)**：
```
FMA(A, B, C, α, β) = α·A·B + β·C

特殊情况：
- 逐元素乘法：β=0      → α·A·B
- 线性组合：A=1, B=α  → α + β·C
```

### 5.2 标量实现

```c
// 加权和
void wsum_serial(
    float const* a, float const* b, size_t n,
    float alpha, float beta, float* result) {

    for (size_t i = 0; i < n; ++i) {
        result[i] = alpha * a[i] + beta * b[i];
    }
}

// FMA
void fma_serial(
    float const* a, float const* b, float const* c, size_t n,
    float alpha, float beta, float* result) {

    for (size_t i = 0; i < n; ++i) {
        result[i] = alpha * a[i] * b[i] + beta * c[i];
    }
}
```

### 5.3 NEON实现

```c
SIMSIMD_PUBLIC void simsimd_wsum_f32_neon(
    simsimd_f32_t const* a,
    simsimd_f32_t const* b,
    simsimd_size_t n,
    simsimd_distance_t alpha,
    simsimd_distance_t beta,
    simsimd_f32_t* result) {

    // 广播标量到向量
    float32x4_t alpha_vec = vdupq_n_f32((float)alpha);
    float32x4_t beta_vec = vdupq_n_f32((float)beta);

    simsimd_size_t i = 0;

    // 主循环：每次4个元素
    for (; i + 4 <= n; i += 4) {
        float32x4_t a_vec = vld1q_f32(a + i);
        float32x4_t b_vec = vld1q_f32(b + i);

        // result = alpha * a + beta * b
        float32x4_t result_vec = vmulq_f32(alpha_vec, a_vec);
        result_vec = vfmaq_f32(result_vec, beta_vec, b_vec);

        vst1q_f32(result + i, result_vec);
    }

    // 尾部
    for (; i < n; ++i) {
        result[i] = alpha * a[i] + beta * b[i];
    }
}

SIMSIMD_PUBLIC void simsimd_fma_f32_neon(
    simsimd_f32_t const* a,
    simsimd_f32_t const* b,
    simsimd_f32_t const* c,
    simsimd_size_t n,
    simsimd_distance_t alpha,
    simsimd_distance_t beta,
    simsimd_f32_t* result) {

    float32x4_t alpha_vec = vdupq_n_f32((float)alpha);
    float32x4_t beta_vec = vdupq_n_f32((float)beta);

    simsimd_size_t i = 0;

    for (; i + 4 <= n; i += 4) {
        float32x4_t a_vec = vld1q_f32(a + i);
        float32x4_t b_vec = vld1q_f32(b + i);
        float32x4_t c_vec = vld1q_f32(c + i);

        // result = alpha * a * b + beta * c
        float32x4_t ab = vmulq_f32(a_vec, b_vec);
        float32x4_t result_vec = vmulq_f32(alpha_vec, ab);
        result_vec = vfmaq_f32(result_vec, beta_vec, c_vec);

        vst1q_f32(result + i, result_vec);
    }

    for (; i < n; ++i) {
        result[i] = alpha * a[i] * b[i] + beta * c[i];
    }
}
```

### 5.4 混合精度支持

**挑战**：输入可能是f16/bf16/i8，输出也要是相同类型

```c
// f16版本：内部使用f32计算
SIMSIMD_PUBLIC void simsimd_wsum_f16_neon(
    simsimd_f16_t const* a,
    simsimd_f16_t const* b,
    simsimd_size_t n,
    simsimd_distance_t alpha,
    simsimd_distance_t beta,
    simsimd_f16_t* result) {

    float32x4_t alpha_vec = vdupq_n_f32((float)alpha);
    float32x4_t beta_vec = vdupq_n_f32((float)beta);

    simsimd_size_t i = 0;

    for (; i + 4 <= n; i += 4) {
        // f16 → f32
        float16x4_t a_f16 = vld1_f16((float16_t const*)(a + i));
        float16x4_t b_f16 = vld1_f16((float16_t const*)(b + i));

        float32x4_t a_vec = vcvt_f32_f16(a_f16);
        float32x4_t b_vec = vcvt_f32_f16(b_f16);

        // 计算（f32）
        float32x4_t result_f32 = vmulq_f32(alpha_vec, a_vec);
        result_f32 = vfmaq_f32(result_f32, beta_vec, b_vec);

        // f32 → f16
        float16x4_t result_f16 = vcvt_f16_f32(result_f32);
        vst1_f16((float16_t*)(result + i), result_f16);
    }

    // 尾部
    for (; i < n; ++i) {
        float ai = simsimd_f16_to_f32(a + i);
        float bi = simsimd_f16_to_f32(b + i);
        float res = alpha * ai + beta * bi;
        simsimd_f32_to_f16(res, result + i);
    }
}
```

---

## 第六部分：统一API设计

### 6.1 SimSIMD的度量类型枚举

```c
// 在simsimd.h中定义
typedef enum {
    // 内积
    simsimd_metric_dot_k = 0,
    simsimd_metric_vdot_k = 1,  // 共轭点积

    // 空间距离
    simsimd_metric_cos_k = 10,
    simsimd_metric_l2sq_k = 11,
    simsimd_metric_l2_k = 12,

    // 概率分布
    simsimd_metric_kl_k = 20,
    simsimd_metric_js_k = 21,

    // 曲线空间
    simsimd_metric_bilinear_k = 30,
    simsimd_metric_mahalanobis_k = 31,

    // 集合操作
    simsimd_metric_hamming_k = 40,
    simsimd_metric_jaccard_k = 41,

    // 稀疏向量
    simsimd_metric_intersect_k = 50,

} simsimd_metric_kind_t;
```

### 6.2 统一的距离计算接口

```c
// 统一接口
simsimd_error_t simsimd_dot(
    void const* a,
    void const* b,
    simsimd_size_t n,
    simsimd_datatype_t datatype,
    simsimd_metric_kind_t metric,
    simsimd_capability_t allowed,
    simsimd_distance_t* result);

// 使用示例
float a[1536], b[1536];
simsimd_distance_t distance;

// KL散度
simsimd_dot(
    a, b, 1536,
    simsimd_datatype_f32_k,
    simsimd_metric_kl_k,
    simsimd_cap_any_k,
    &distance
);

// Cosine距离
simsimd_dot(
    a, b, 1536,
    simsimd_datatype_f32_k,
    simsimd_metric_cos_k,
    simsimd_cap_any_k,
    &distance
);
```

### 6.3 动态分发机制

```c
// 根据CPU能力和度量类型选择最优实现
simsimd_metric_punned_t simsimd_metric_punned(
    simsimd_metric_kind_t kind,
    simsimd_datatype_t datatype,
    simsimd_capability_t allowed) {

    simsimd_capability_t caps = simsimd_capabilities() & allowed;

    // KL散度
    if (kind == simsimd_metric_kl_k) {
        if (datatype == simsimd_datatype_f32_k) {
            #if SIMSIMD_TARGET_SAPPHIRE
            if (caps & simsimd_cap_sapphire_k)
                return (simsimd_metric_punned_t)simsimd_kl_f32_sapphire;
            #endif
            #if SIMSIMD_TARGET_SKYLAKE
            if (caps & simsimd_cap_skylake_k)
                return (simsimd_metric_punned_t)simsimd_kl_f32_skylake;
            #endif
            #if SIMSIMD_TARGET_NEON
            if (caps & simsimd_cap_neon_k)
                return (simsimd_metric_punned_t)simsimd_kl_f32_neon;
            #endif
            return (simsimd_metric_punned_t)simsimd_kl_f32_serial;
        }
    }

    // ... 其他度量类型
}
```

---

## 第七部分：实战与总结

### 7.1 性能对比测试

```c
#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include <math.h>

#define N 1024

// 生成归一化的概率分布
void generate_distribution(float* p, size_t n) {
    float sum = 0.0f;
    for (size_t i = 0; i < n; ++i) {
        p[i] = (float)rand() / RAND_MAX;
        sum += p[i];
    }
    // 归一化
    for (size_t i = 0; i < n; ++i) {
        p[i] /= sum;
    }
}

int main() {
    srand(time(NULL));

    float* p = aligned_alloc(64, N * sizeof(float));
    float* q = aligned_alloc(64, N * sizeof(float));

    generate_distribution(p, N);
    generate_distribution(q, N);

    printf("=== 概率分布距离测试 (n=%d) ===\n\n", N);

    // KL散度
    simsimd_distance_t kl_serial, kl_simd;

    clock_t start = clock();
    for (int i = 0; i < 100000; ++i) {
        simsimd_kl_f32_serial(p, q, N, &kl_serial);
    }
    clock_t end = clock();
    printf("KL散度 (Serial): %.6f\n", kl_serial);
    printf("时间: %.3f ms\n\n", 1000.0 * (end - start) / CLOCKS_PER_SEC);

    start = clock();
    for (int i = 0; i < 100000; ++i) {
        simsimd_kl_f32_neon(p, q, N, &kl_simd);
    }
    end = clock();
    printf("KL散度 (NEON): %.6f\n", kl_simd);
    printf("时间: %.3f ms\n", 1000.0 * (end - start) / CLOCKS_PER_SEC);
    printf("加速比: %.2fx\n\n",
           (double)(end_serial - start_serial) / (end - start));

    // JS散度
    simsimd_distance_t js_serial, js_simd;
    simsimd_js_f32_serial(p, q, N, &js_serial);
    simsimd_js_f32_neon(p, q, N, &js_simd);

    printf("JS散度 (Serial): %.6f\n", js_serial);
    printf("JS散度 (NEON): %.6f\n", js_simd);
    printf("误差: %.6e\n", fabs(js_serial - js_simd));

    free(p);
    free(q);
    return 0;
}
```

### 7.2 今日学习要点

1. ✅ **概率分布距离**：KL散度、JS散度的定义和应用
2. ✅ **对数近似**：多项式逼近、SIMD实现
3. ✅ **曲线空间度量**：Mahalanobis距离、双线性形式
4. ✅ **Element-wise操作**：加权和、FMA
5. ✅ **统一API**：动态分发、跨度量类型设计

### 7.3 作业

#### 作业1：实现log10
基于log2实现，编写SIMD版本的log10函数（提示：log10(x) = log2(x) / log2(10)）。

#### 作业2：对比精度
测试SIMD log2近似与标准库log2f的精度差异，绘制误差分布图。

#### 作业3：Mahalanobis距离
实现完整的Mahalanobis距离，包括NEON和AVX2版本。

#### 作业4：混合精度WSum
实现i8→f16→i8的加权和函数，分析精度损失。

#### 作业5：阅读源码
```bash
# 深入研究概率分布实现
less /home/dev/SimSIMD/include/simsimd/probability.h

# 查看曲线空间实现
less /home/dev/SimSIMD/include/simsimd/curved.h

# 查看element-wise实现
less /home/dev/SimSIMD/include/simsimd/elementwise.h
```

### 预习第7天内容

明天是最后一天，我们将学习：
- **Python/Rust/JavaScript绑定**
- **性能测试与benchmark**
- **实际应用案例分析**
- **SimSIMD的未来发展方向**
- **综合项目实战**

---

**下一课**：[第7天：语言绑定与综合实战](./第7天_语言绑定与综合实战.md)

## 参考资源

- [KL Divergence](https://en.wikipedia.org/wiki/Kullback%E2%80%93Leibler_divergence)
- [Jensen-Shannon Divergence](https://en.wikipedia.org/wiki/Jensen%E2%80%93Shannon_divergence)
- [Mahalanobis Distance](https://en.wikipedia.org/wiki/Mahalanobis_distance)
- [Fast Logarithm Approximation](https://github.com/romeric/fastapprox)
- [SIMD Math Library](https://sleef.org/)
