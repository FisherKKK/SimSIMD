# 补充课程5：量化技术与混合精度计算深度剖析

## 课程目标
- 掌握 IEEE 754 各精度浮点格式的位布局
- 理解 SimSIMD 中 f16/bf16/i8 的软件模拟转换原理
- 深入分析 Quake 3 快速平方根倒数算法（rsqrt）
- 理解 `SIMSIMD_STATIC_ASSERT` 编译期类型检查技巧
- 掌握混合精度计算的权衡：精度 vs 速度 vs 内存

**核心源码**：`include/simsimd/types.h`（644行）

---

## 第一部分：为什么需要量化？

### 1.1 向量维度与内存的矛盾

```
现代向量嵌入的典型规模：

模型             维度      f32 内存/向量    向量数量    总内存
─────────────────────────────────────────────────────────────
text-embedding   1536      6 KB            1亿条       600 TB
OpenAI ADA-002   1536      6 KB            1亿条       600 TB
CLIP ViT-L/14     768      3 KB            1亿条       300 TB

量化后：
─────────────────────────────────────────────────────────────
f16（2字节）     1536      3 KB     → 300 TB（节省50%）
i8 （1字节）     1536      1.5 KB   → 150 TB（节省75%）
b1 （1位）       1536      192 B    → 18.75 TB（节省97%）

量化是大规模向量搜索的必要条件！
```

### 1.2 精度 vs 性能的权衡

```
数据类型    宽度    精度         SIMD 并行度    AI 支持
─────────────────────────────────────────────────────
f64         64位    15位有效十进制  4个/256位 AVX2    有限
f32         32位    7位有效十进制   8个/256位 AVX2    广泛
f16         16位    3-4位有效十进制 16个/256位 AVX2   广泛（推理）
bf16        16位    2-3位有效十进制 16个/256位 AVX2   广泛（训练）
i8          8位     整数 [-128,127] 32个/256位 AVX2   推理专用
i4          4位     整数 [-8,7]     64个/256位 AVX2   激进量化
```

---

## 第二部分：IEEE 754 浮点数格式全解

### 2.1 格式一览

```
f64（64位 double）：
  ┌─┬─────────────┬────────────────────────────────────────────────┐
  │S│  Exponent   │                   Mantissa                     │
  │1│     11      │                      52                        │
  └─┴─────────────┴────────────────────────────────────────────────┘
  偏置量：1023   最大正规指数：1023   精度：~15位十进制

f32（32位 float）：
  ┌─┬──────────┬───────────────────────┐
  │S│ Exponent │       Mantissa        │
  │1│     8    │          23           │
  └─┴──────────┴───────────────────────┘
  偏置量：127    最大正规指数：127    精度：~7位十进制

f16（16位 IEEE半精度）：
  ┌─┬──────┬──────────┐
  │S│ Exp  │ Mantissa │
  │1│  5   │    10    │
  └─┴──────┴──────────┘
  偏置量：15     最大正规指数：15    精度：~3.3位十进制

bf16（16位 Brain Float）：
  ┌─┬──────────┬───────┐
  │S│ Exponent │  Man. │
  │1│     8    │   7   │
  └─┴──────────┴───────┘
  偏置量：127    与 f32 相同的指数范围   精度：~2位十进制
```

**关键差异**：
```
f16：指数位少（5 vs 8），尾数位多（10 vs 7）
  → 范围小（±65504），精度相对高
  → 溢出风险：大数值训练容易变 inf

bf16：与 f32 相同的指数位（8），尾数位大幅减少（7 vs 23）
  → 范围与 f32 相同（±3.4×10^38），精度低
  → 不会在 f32 范围内溢出
  → Google TPU 专为训练设计，现 PyTorch 广泛使用
```

### 2.2 特殊值的位表示

```
f32 特殊值：
  0.0       = 0 00000000 00000000000000000000000
  -0.0      = 1 00000000 00000000000000000000000
  +inf      = 0 11111111 00000000000000000000000
  -inf      = 1 11111111 00000000000000000000000
  NaN       = ? 11111111 非零尾数
  Denormal  = 0 00000000 非零尾数（指数=0的特殊情况）

f16 等价：
  +inf      = 0 11111 0000000000
  最大正规数 = 0 11110 1111111111 = 65504.0
  NaN       = 0 11111 非零尾数
```

---

## 第三部分：f16 ↔ f32 转换的位操作魔法

### 3.1 软件 f16→f32 转换

```c
// types.h:568-578
SIMSIMD_INTERNAL simsimd_f32_t simsimd_f16_to_f32(simsimd_f16_t const *x_ptr) {
    unsigned short x = *(unsigned short const *)x_ptr;
    //             ^                  ^^^^^^^^^^^^^^^^^^^
    //             16位无符号读取       通过指针读取实际位模式

    // 提取 f16 各字段
    unsigned int exponent = (x & 0x7C00) >> 10;
    //                           0x7C00 = 0111 1100 0000 0000
    //                           提取 bit 14:10（5位指数）
    //                           >> 10 移到低位

    unsigned int mantissa = (x & 0x03FF) << 13;
    //                           0x03FF = 0000 0011 1111 1111
    //                           提取 bit 9:0（10位尾数）
    //                           << 13 移到 f32 尾数的高位位置

    simsimd_f32i32_t mantissa_conv;
    mantissa_conv.f = (float)mantissa;    // 将尾数当作整数转换为float
    unsigned int v = (mantissa_conv.i) >> 23;
    // v：尾数整数值的 f32 指数部分（用于处理非规格化 f16）

    simsimd_f32i32_t conv;
    conv.i =
        (x & 0x8000) << 16
        //  0x8000 = 1000 0000 0000 0000（符号位）
        //  << 16 移到 f32 的 bit 31（符号位）
        |
        (exponent != 0) * ((exponent + 112) << 23 | mantissa)
        //  正规数分支（指数≠0）：
        //  f16 偏置 = 15，f32 偏置 = 127，差 = 112
        //  (exponent + 112) << 23：重新偏置并放到 f32 指数位
        //  | mantissa：附上尾数
        |
        ((exponent == 0) & (mantissa != 0)) *
            ((v - 37) << 23 | ((mantissa << (150 - v)) & 0x007FE000));
        //  非规格化数分支（指数=0，尾数≠0）：
        //  需要找到尾数的最高有效位，计算等价的正规化指数
        //  37 = 127 - 90，90 是非规格化数的偏置调整
    return conv.f;
}
```

**三种情况的处理**：

```
情况1：正规数（指数≠0）
  f16: 0 01111 0000000000 → 1.0
  处理：
    exponent = 15
    f32 exponent = 15 + 112 = 127 → f32偏置127 = 2^0 = 1.0
    mantissa 扩展：10位 → 23位（右对齐，低位补0）
  结果：0 01111111 00000000000000000000000 = 1.0f

情况2：零值（指数=0，尾数=0）
  (exponent != 0) = 0，第二项为0
  ((exponent == 0) & (mantissa != 0)) = 0，第三项为0
  结果：只有符号位 = ±0.0f

情况3：非规格化 f16（指数=0，尾数≠0）
  f16 非规格化值，需要转为 f32 正规数
  步骤：找最高有效位位置，计算新指数，规格化尾数
  使用 log2(mantissa) 的浮点表示来快速找位位置
```

### 3.2 f32→f16 压缩

```c
// types.h:590-599
SIMSIMD_INTERNAL void simsimd_f32_to_f16(simsimd_f32_t x, simsimd_f16_t *result_ptr) {
    simsimd_f32i32_t conv;
    conv.f = x;

    unsigned int b = conv.i + 0x00001000;
    //                        0x1000 = 尾数的 bit 12
    //                        在截断（丢弃低13位）前进行四舍五入
    //                        当 bit 12 以下 ≥ 0.5 时，这会触发进位

    unsigned int e = (b & 0x7F800000) >> 23;
    //                   提取 f32 指数（加了舍入偏置后）

    unsigned int m = b & 0x007FFFFF;
    //                  提取 f32 尾数（加了舍入偏置后）

    unsigned short result =
        ((b & 0x80000000) >> 16)    // 符号位：f32 bit 31 → f16 bit 15
        |
        (e > 112) * (((( e - 112) << 10) & 0x7C00) | (m >> 13))
        //            ^^^^^^^^^^^          ^^^^^^^^^^^  ^^^^^^^
        //            f32偏置127 - f16偏置15 = 112
        //            正规 f32 → 正规 f16（e > 112 表示 f32 的真实指数 ≥ -14）
        //            << 10 放到 f16 指数位
        //            m >> 13：保留高10位尾数
        |
        ((e < 113) & (e > 101)) *
            ((((0x007FF000 + m) >> (125 - e)) + 1) >> 1)
        //   非规格化 f16：f32的正规小数→f16的非规格化表示
        |
        ((e > 143) * 0x7FFF);
        //  溢出：f32值超过 f16 范围（e > 143 = 127 + 16）→ +inf (0x7C00) or 最大有限值
        //  这里选 0x7FFF（NaN 的一种，也可以选 0x7C00=+inf）

    *(unsigned short *)result_ptr = result;
}
```

**舍入技巧**：
```
0x00001000 的作用（四舍五入）：
  f32 尾数 23 位，f16 尾数 10 位，需要截断低 13 位

  截断前：xxxx xxxx xxxx x|yyy yyyy yyyy yyyy  （|处截断）
                          ^ bit 12

  +0x1000（bit12为1的值）：
  如果 bit 11:0 >= 0x800（≥0.5）：触发进位，等效于"向上舍入"
  如果 bit 11:0 < 0x800（<0.5）：不进位，等效于"向下截断"

  这是"四舍五入到最近偶数"（round-half-to-even）的近似实现
```

### 3.3 bf16 转换的惊人简单性

```c
// types.h:609-630
// bf16 → f32：仅仅是左移16位！
SIMSIMD_INTERNAL simsimd_f32_t simsimd_bf16_to_f32(simsimd_bf16_t const *x_ptr) {
    unsigned short x = *(unsigned short const *)x_ptr;
    simsimd_f32i32_t conv;
    conv.i = x << 16;  // ← 就这一行！
    //       ^^^^^^
    //       bf16 的位布局 = f32 的高16位！
    //       左移16：将 bf16 扩展为 f32，低16位（尾数后半部分）补0
    return conv.f;
}

// f32 → bf16：仅仅是右移16位！
SIMSIMD_INTERNAL void simsimd_f32_to_bf16(simsimd_f32_t x, simsimd_bf16_t *result_ptr) {
    simsimd_f32i32_t conv;
    conv.f = x;
    conv.i += 0x8000;  // 可选：四舍五入（对bit 15 之后进行舍入）
    //        ^^^^^^
    //        bit 15 的进位 = 对 bf16 的尾数第1位（即 bit 16）进行舍入
    conv.i >>= 16;     // 右移16位，保留高16位
    *(unsigned short *)result_ptr = (unsigned short)conv.i;
}
```

**bf16 与 f32 的关系**：
```
f32：  [S:1][E:8][M:23]
bf16： [S:1][E:8][M:7]

bf16 = f32 高16位（指数完全保留，尾数截断）

内存中：
f32  = 3F800000h = 0011 1111 1000 0000 0000 0000 0000 0000 = 1.0
bf16 = 3F80h     = 0011 1111 1000 0000                    = 1.0

只需要一次 16 位移位！而 f16↔f32 需要复杂的指数重新偏置。
这就是为什么 bf16 的转换硬件极其简单，被 TPU/GPU 广泛采用。
```

---

## 第四部分：整数量化（i8/i4x2）

### 4.1 i8 量化宏

```c
// types.h:462-477
// f32 → i8 量化（带饱和截断和四舍五入）
#define SIMSIMD_F32_TO_I8(x, y) \
    *(y) = (simsimd_i8_t)(                              \
        (x) > 127                                        \
        ? 127                                            \
        : ((x) < -128                                    \
           ? -128                                        \
           : (int)((x) + ((x) < 0 ? -0.5f : 0.5f))     \
          )                                              \
    )
//  ^^^^^^ 分解：
//  1. 饱和：> 127 → 127，< -128 → -128（防止截断溢出）
//  2. 四舍五入：
//     正数：(x + 0.5) 截断 → 向上舍入
//     负数：(x - 0.5) 截断 → 向负无穷舍入（banker's rounding的近似）
//  3. 最终强转：(int) 截断小数，然后 (simsimd_i8_t) 截断高位
```

**量化误差分析**：
```
量化步骤：
  1. 原始值：0.7321
  2. 缩放（假设量化范围 [-1,1] 对应 [-127,127]）：
     0.7321 × 127 ≈ 92.97
  3. 四舍五入：93
  4. 反量化：93 / 127 ≈ 0.7323

  量化误差 ≈ 0.0002（< 1/127 ≈ 0.008 的 LSB）

精度代价：
  f32：7 位有效十进制
  i8：约 2.3 位有效十进制（log10(256) ≈ 2.41）
  精度损失：约 4.7 位十进制 = 56 dB 信噪比损失

  对于余弦相似度：通常 i8 量化的误差 < 0.01（1%）
  对于大多数搜索应用：可接受！
```

### 4.2 i4x2：两个4位整数打包

```c
// types.h
typedef unsigned char simsimd_i4x2_t;  // 1字节存2个4位整数

// 打包格式（模拟）：
//   high nibble：第一个 i4   [7:4]
//   low  nibble：第二个 i4   [3:0]

// 解包：
int8_t extract_high(simsimd_i4x2_t packed) {
    int8_t val = (packed >> 4) & 0x0F;
    // 符号扩展：4位有符号 → 8位有符号
    if (val & 0x08) val |= 0xF0;  // 如果 bit 3 是1，扩展符号
    return val;
}

int8_t extract_low(simsimd_i4x2_t packed) {
    int8_t val = packed & 0x0F;
    if (val & 0x08) val |= 0xF0;
    return val;
}
```

**i4 的 SIMD 处理**：
```
AVX-512 的 i4 支持（通过 nibble 分离）：
  1. 取低4位：_mm512_and_si512(data, _mm512_set1_epi8(0x0F))
  2. 取高4位：_mm512_srli_epi16(data, 4)
  3. 符号扩展：通过 shuffle/vpshufb 查表

Intel Sapphire Rapids 新增 VNNI i4 支持：
  _mm512_dpbssd_epi32（i8×i8 累加，但 i4 需要先转换）
  QVNNI（未来指令）：原生 i4 矩阵乘法
```

---

## 第五部分：f32i32 Union 类型 - 类型双关的核心工具

### 5.1 联合体定义

```c
// types.h:480-483
typedef union {
    unsigned i;   // 以整数视角读写
    float f;      // 以浮点视角读写
} simsimd_f32i32_t;
```

**为什么这是合法的？**：
```c
// C 标准：通过 union 进行类型双关是明确定义的（C99 §6.5.2.3）
simsimd_f32i32_t conv;
conv.f = 1.0f;               // 以 float 写入
unsigned bits = conv.i;      // 以 int 读取 → 合法！

// 等价的（但在 C++ 中技术上是 UB）：
float f = 1.0f;
unsigned bits;
memcpy(&bits, &f, 4);        // 用 memcpy 绕过严格别名规则（也合法）

// 非法（严格别名违规，多数编译器警告）：
float f = 1.0f;
unsigned bits = *(unsigned*)&f;   // UB！
```

### 5.2 如何利用 union 操作浮点位

```c
// 实际示例：检查一个 float 是否是 NaN
int is_nan(float x) {
    simsimd_f32i32_t conv;
    conv.f = x;
    // NaN：指数全1（bit 30:23 = 0xFF），尾数非0（bit 22:0 ≠ 0）
    return ((conv.i & 0x7F800000) == 0x7F800000) && (conv.i & 0x007FFFFF);
}

// 快速绝对值（清除符号位）
float fast_abs(float x) {
    simsimd_f32i32_t conv;
    conv.f = x;
    conv.i &= 0x7FFFFFFF;  // 清除 bit 31（符号位）
    return conv.f;
}

// 快速符号翻转（翻转符号位）
float fast_neg(float x) {
    simsimd_f32i32_t conv;
    conv.f = x;
    conv.i ^= 0x80000000;  // 翻转 bit 31（符号位）
    return conv.f;
}
// 这就是 补充2 中提到的 SIMD vxorps 优化的标量版本！
```

---

## 第六部分：Quake 3 快速平方根倒数

### 6.1 算法原理

```c
// types.h:523-530
SIMSIMD_INTERNAL simsimd_f32_t simsimd_approximate_inverse_square_root(simsimd_f32_t number) {
    simsimd_f32i32_t conv;
    conv.f = number;
    conv.i = 0x5F1FFFF9 - (conv.i >> 1);
    //       ^^^^^^^^^^^   ^^^^^^^^^^^
    //       魔数           位移近似 log2(x)
    //
    //  数学原理：
    //  设 x = 浮点数，其位表示约等于 2^23 × (log2(x) + 127)
    //  则 x >> 1 ≈ 2^23 × (log2(x)/2 + 127/2)
    //             ≈ 2^23 × (log2(√x) + 63.5)
    //
    //  Magic - (x >> 1)
    //  ≈ 2^23 × (log2(1/√x) + 127)
    //  ≈ float(1/√x)
    //
    //  0x5F1FFFF9 是比原版 Quake (0x5F3759DF) 更精确的魔数
    //  由 Jan Kadlec 在 2010 年发现

    // Newton-Raphson 迭代：提升精度
    // 设 y₀ = 初始近似，目标 f(y) = 1/y² - x = 0
    // y₁ = y₀ × (1.5 - x/2 × y₀²)
    //
    // SimSIMD 使用的变形版本：
    conv.f *= 0.703952253f * (2.38924456f - number * conv.f * conv.f);
    //       ↑ 系数来自 Jan Kadlec 的优化，比 Quake 原版精度更高

    return conv.f;
}
```

**数学推导（简化版）**：
```
浮点数 x 的 32 位表示：
  bits(x) = (1 + m) × 2^(e-127) 的 IEEE 编码
  bits(x) ≈ 2^23 × (e-127 + log2(1+m))
          ≈ 2^23 × (log2(x) + 127 - 1)  [近似 log2(1+m) ≈ m]

求 1/√x = x^(-1/2) 的 bits：
  bits(1/√x) ≈ 2^23 × (log2(x^(-1/2)) + 127 - 1)
             = 2^23 × (-1/2 × log2(x) + 126)
             = 2^23 × 126 - (2^23 × log2(x)) / 2
             = 0x3F000000 × 2 - bits(x)/2
             ≈ Magic - bits(x) >> 1

Magic = 0x3F000000 × 2 × 2^22 + 修正项 = 0x5F400000 + 修正
实验得最优 Magic ≈ 0x5F1FFFF9（Jan Kadlec, 2010）
```

**现代实际价值**：
```
历史意义（1990年代）：
  软件 sqrt：10~20 周期
  rsqrt magic number：约 10 周期
  近似精度：~0.1% 误差（通常足够）

现代等价：
  硬件 rsqrtss（SSE1，1999）：0.85ns，误差 ~0.00094%
  Newton-Raphson 迭代后：与 1/sqrt() 精度相同

SimSIMD 的用途：
  - 当 SSE/NEON 不可用时的串行后备
  - 理解 rsqrt 工作原理的最佳代码示例
  - 余弦距离归一化步骤（normalize → dot product）
```

### 6.2 Newton-Raphson 迭代

```c
// 经典 Quake 3 版本（对比）：
float Q_rsqrt(float number) {
    long i;
    float x2, y;
    const float threehalfs = 1.5F;

    x2 = number * 0.5F;
    y  = number;
    i  = *(long *) &y;          // 类型双关
    i  = 0x5f3759df - ( i >> 1 );
    y  = *(float *) &i;         // 类型双关（UB in modern C++！）
    y  = y * ( threehalfs - ( x2 * y * y ) );  // 1次 Newton-Raphson
    // y  = y * ( threehalfs - ( x2 * y * y ) );  // 2次（可选，更精确）

    return y;
}

// Newton-Raphson 原理：
// 求 f(y) = 0，其中 f(y) = 1/y² - x（x = 输入，y = 1/√x 近似）
// 迭代公式：y₁ = y₀ - f(y₀)/f'(y₀)
//          = y₀ - (1/y₀² - x) / (-2/y₀³)
//          = y₀ + (1/y₀² - x) × y₀³/2
//          = y₀ × (3/2 - x/2 × y₀²)
//          = y₀ × (1.5 - x × 0.5 × y₀²)
//
// 每次迭代大约将精度翻倍（二次收敛）
```

---

## 第七部分：对数近似与 KL 散度优化

### 7.1 Mercator 级数近似 log

```c
// types.h:550-554
SIMSIMD_INTERNAL simsimd_f32_t simsimd_approximate_log(simsimd_f32_t number) {
    simsimd_f32_t x = number - 1;      // 使参数靠近 0
    simsimd_f32_t x2 = x * x;
    simsimd_f32_t x3 = x * x * x;
    return x - x2 / 2 + x3 / 3;
    //     ^^^^^^^^^^^^^^^^^^^^^^^^^
    //     Mercator 级数：ln(1+x) = x - x²/2 + x³/3 - x⁴/4 + ...
    //     截断到 3 阶：在 x ∈ [-0.5, 0.5] 内精度约 ±0.01
}
```

**精度分析**：
```
精确值：ln(0.9) = -0.10536...
Mercator 3阶：0.9-1=(-0.1), -0.1 - 0.01/2 + (-0.001)/3 = -0.10533...
误差：0.003%

精确值：ln(0.5) = -0.69315...
Mercator 3阶：-0.5 - 0.25/2 + (-0.125)/3 = -0.5 - 0.125 - 0.0417 = -0.6667
误差：3.8%（x=-0.5时精度明显下降）

结论：仅在参数接近 1 时精度够用，适合 KL 散度（概率值通常接近 1/N）
更高精度需要更多项或使用 SIMD 指令（如 vlog）
```

---

## 第八部分：SIMSIMD_STATIC_ASSERT - 编译期类型安全

### 8.1 技巧原理

```c
// types.h:410-411
// 定义宏（注意是 typedef char 数组）
#define SIMSIMD_STATIC_ASSERT(cond, msg) \
    typedef char static_assertion_##msg[(cond) ? 1 : -1]
//  ^^^^^^^^                             ^^^^^^^^^^^^^^^^
//  typedef char array                   条件为真→长度1，假→长度-1（非法！）
//  静态（编译期）               ##：token粘贴，msg展开成名称的一部分

// 使用：检查类型大小
SIMSIMD_STATIC_ASSERT(sizeof(simsimd_b8_t)  == 1, simsimd_b8_t_must_be_1_byte);
SIMSIMD_STATIC_ASSERT(sizeof(simsimd_f16_t) == 2, simsimd_f16_t_must_be_2_bytes);
SIMSIMD_STATIC_ASSERT(sizeof(simsimd_f32_t) == 4, simsimd_f32_t_must_be_4_bytes);
SIMSIMD_STATIC_ASSERT(sizeof(simsimd_f64_t) == 8, simsimd_f64_t_must_be_8_bytes);
```

**展开示例**：
```c
// 展开：SIMSIMD_STATIC_ASSERT(sizeof(simsimd_f32_t) == 4, simsimd_f32_t_must_be_4_bytes)
typedef char static_assertion_simsimd_f32_t_must_be_4_bytes[sizeof(float) == 4 ? 1 : -1];

// 如果 sizeof(float) == 4（通常如此）：
typedef char static_assertion_simsimd_f32_t_must_be_4_bytes[1];  // 合法
// 编译成功！

// 如果某种奇怪平台 sizeof(float) != 4（比如16位DSP）：
typedef char static_assertion_simsimd_f32_t_must_be_4_bytes[-1]; // 非法！
// 编译错误：array size must be positive
// 错误信息中包含 "simsimd_f32_t_must_be_4_bytes"，一眼看出原因！
```

**与 C11 `_Static_assert` 的对比**：
```c
// C11 方式（C11 之后推荐）：
_Static_assert(sizeof(float) == 4, "float must be 4 bytes");

// SimSIMD 的宏方式（C99 兼容）：
SIMSIMD_STATIC_ASSERT(sizeof(float) == 4, float_must_be_4_bytes);

// 差异：
// C11：提供清晰的字符串错误信息
// 宏方式：C99 兼容，消息作为变量名（可读性略差但无依赖）

// C11 前 SimSIMD 也是如此兼容各种老编译器
```

---

## 第九部分：复数类型与 SIMD 存储布局

### 9.1 复数类型定义

```c
// types.h:486-507
typedef struct { simsimd_f16_t  real; simsimd_f16_t  imag; } simsimd_f16c_t;
typedef struct { simsimd_bf16_t real; simsimd_bf16_t imag; } simsimd_bf16c_t;
typedef struct { simsimd_f32_t  real; simsimd_f32_t  imag; } simsimd_f32c_t;
typedef struct { simsimd_f64_t  real; simsimd_f64_t  imag; } simsimd_f64c_t;
```

**内存布局（交错格式 AOS）**：
```
内存中：[re₀, im₀, re₁, im₁, re₂, im₂, ...]

对比 Python/NumPy 的 complex64：
  numpy.complex64 = 2 × float32 = 8字节/复数
  内存布局：[re₀, im₀, re₁, im₁, ...] ← 与 simsimd_f32c_t 完全兼容！

SIMD 处理复数向量的挑战：
  向量寄存器中自然是：[re₀, im₀, re₁, im₁, re₂, im₂, re₃, im₃]
  但 FMA 需要：[re_a, re_a, re_a, re_a] 和 [im_a, im_a, im_a, im_a]

  解决方案（见 dot.h 中的复数点积）：
  1. 加载交错数据
  2. 用 _mm256_moveldup_ps 复制实部：[r, r, r, r, r, r, r, r]
  3. 用 _mm256_movehdup_ps 复制虚部：[i, i, i, i, i, i, i, i]
  4. 分别做 FMA
  5. 用 _mm256_addsub_ps 合并：[r×r+i×i, r×i-i×r, ...]
```

---

## 第十部分：类型转换宏设计哲学

### 10.1 原生支持与软件模拟的抽象

```c
// types.h:436-458
// 条件编译的宏抽象层
#if SIMSIMD_NATIVE_F16
    // 编译器原生支持 _Float16 / __fp16
    #define SIMSIMD_F16_TO_F32(x) (SIMSIMD_DEREFERENCE(x))
    //                            ↑ 编译器直接转换，0开销
    #define SIMSIMD_F32_TO_F16(x, y) (SIMSIMD_EXPORT(x, y))
#else
    // 软件模拟（通过位操作）
    #define SIMSIMD_F16_TO_F32(x) (simsimd_f16_to_f32(x))
    //                            ↑ 调用上面分析的位操作函数
    #define SIMSIMD_F32_TO_F16(x, y) (simsimd_f32_to_f16(x, y))
#endif

// SIMSIMD_DEREFERENCE 和 SIMSIMD_EXPORT 是简单的取值/赋值宏：
#define SIMSIMD_DEREFERENCE(x) (*(x))  // 取指针指向的值
#define SIMSIMD_EXPORT(x, y) *(y) = x  // 将值写入指针位置
```

**这种抽象的好处**：
```
使用者代码（spatial.h 中的 cosine 等）：
  simsimd_f32_t a_val = SIMSIMD_F16_TO_F32(a_ptr + i);

无论平台如何，这一行代码：
  - 支持原生 f16 的编译器：0 开销，编译器直接转换
  - 不支持的编译器：调用软件转换，保证正确性
  - SIMD 版本（haswell 等）：使用 _mm256_cvtph_ps 硬件转换

单行代码，三种实现，对使用者完全透明！
```

### 10.2 SIMSIMD_NATIVE_F16 检测

```c
// types.h:310-328
#if (defined(__GNUC__) || defined(__clang__)) && \
    (defined(__ARM_ARCH) || defined(__aarch64__)) && \
    (defined(__ARM_FP16_FORMAT_IEEE))
    // ARM + IEEE fp16 格式（区别于 alternative format）
    typedef __fp16 simsimd_f16_t;
    #define SIMSIMD_NATIVE_F16 1

#elif defined(__AVX512FP16__)
    // x86 Sapphire Rapids：原生 _Float16
    typedef _Float16 simsimd_f16_t;
    #define SIMSIMD_NATIVE_F16 1

#else
    // 所有其他平台：用 unsigned short 存储位，软件转换
    typedef unsigned short simsimd_f16_t;
    #define SIMSIMD_NATIVE_F16 0
#endif
```

**Apple 的特殊情况**：
```
Apple M1/M2/M3 → Armv8.5/8.6 → 有 __ARM_NEON 但没有 __ARM_FP16_FORMAT_IEEE
原因：Apple 使用 "alternative" fp16 格式（精度相同，NaN/inf 处理不同）

结果：Apple Silicon 上：
  SIMSIMD_NATIVE_F16 = 0（软件转换）
  但 NEON 有 vcvt_f32_f16 等指令可以正确转换
  SIMD 版本仍然高效！

对比 Android ARM：
  SIMSIMD_NATIVE_F16 = 1（原生支持）
  直接用 __fp16 类型算术运算
```

---

## 第十一部分：位旋转工具函数

### 11.1 循环左移/右移

```c
// types.h:633-638
SIMSIMD_INTERNAL simsimd_u32_t simsimd_u32_rol(simsimd_u32_t x, int n) {
    return (x << n) | (x >> (32 - n));
}
SIMSIMD_INTERNAL simsimd_u32_t simsimd_u32_ror(simsimd_u32_t x, int n) {
    return (x >> n) | (x << (32 - n));
}
// 同理有 u16 和 u8 版本
```

**用途**：
```
循环移位主要用于：
  1. 哈希函数（如 MurmurHash、CityHash）
  2. 密码学（如 AES、SHA）
  3. 数据布局调整

在 SimSIMD 中，可能用于：
  - 预处理二进制向量中的位排列
  - 哈希辅助函数

x86：有专用指令 ROL/ROR
  __builtin_rotateleft32(x, n)  → GCC/Clang 直接生成 ROL
ARM：有 ROR 指令
  ror 指令 → ARMv6+ 支持
```

---

## 第十二部分：混合精度计算实战

### 12.1 常见混合精度模式

```
模式1：存储 f16，计算 f32（最常见）
  │
  ├── 加载：_mm256_cvtph_ps(f16_vec) → f32x8
  ├── 计算：_mm256_fmadd_ps(a, b, acc) → f32 累加器
  └── 存储：保持 f32 最终结果

模式2：存储 bf16，计算 f32
  │
  ├── 加载：_mm256_loadu_si256 → 256 bits bf16
  ├── 解包：左移16位 → f32（就是 bf16→f32 的向量版本）
  ├── 计算：f32 FMA
  └── 存储：结果下转 bf16

模式3：存储 i8，计算 i32（整数精确）
  │
  ├── 加载：_mm256_loadu_si256 → i8×32
  ├── 计算：_mm256_maddubs_epi16 → i8 dot product → i16
  ├── 扩展：_mm256_madd_epi16 → i16 × i16 → i32
  └── 累加：_mm256_add_epi32
```

### 12.2 哪种量化适合哪种应用

```
┌──────────┬────────────────┬────────────────────┬──────────────────┐
│ 量化类型  │ 精度            │ 适用场景            │ 不适用场景        │
├──────────┼────────────────┼────────────────────┼──────────────────┤
│ f32      │ 7 位十进制      │ 精确计算、训练       │ 内存受限          │
│ f16      │ 3-4 位十进制   │ 推理、cosine搜索    │ 数值不稳定场景    │
│ bf16     │ 2-3 位十进制   │ 训练、GPU推理       │ 精度敏感计算     │
│ i8       │ 2 位十进制      │ 高速推理、向量数据库 │ 高精度需求       │
│ i4       │ 1 位十进制      │ 极度压缩，专用硬件  │ 通用搜索         │
│ b1       │ 1bit/维度      │ 哈希检索、粗筛选    │ 精确搜索         │
└──────────┴────────────────┴────────────────────┴──────────────────┘

向量搜索的最佳实践：
  1. 训练索引：f32（精度优先）
  2. 存储向量：f16 或 i8（内存优先）
  3. 计算距离：f32 累加器（精度+速度平衡）
```

---

## 总结

### 核心知识点

```
1. 格式关系：
   bf16 = f32 高16位（极简转换：移位）
   f16 ≠ f32 的截断（需要指数重新偏置：复杂转换）

2. 精度对比：
   f32（23位尾数）> f16（10位尾数）> bf16（7位尾数）
   范围对比：
   f32 ≈ bf16（相同的指数位）>> f16（5位指数）

3. 量化要点：
   f32→i8：饱和（防溢出）+ 四舍五入（减误差）
   关键：SIMSIMD_F32_TO_I8 宏包含完整的保护逻辑

4. 类型安全：
   SIMSIMD_STATIC_ASSERT：C99 兼容的编译期断言
   typedef char array[-1] 会导致编译错误（妙！）

5. 性能关键：
   simsimd_approximate_inverse_square_root：rsqrt 的完整推导
   SIMSIMD_NATIVE_F16：原生支持时 0 开销，否则软件模拟
```

### SimSIMD 量化设计的一致性

```
统一的抽象宏：
  SIMSIMD_F16_TO_F32(ptr)  // 对所有平台相同的接口
  SIMSIMD_BF16_TO_F32(ptr)
  SIMSIMD_F32_TO_I8(x, ptr)

SIMD 版本的对应优化：
  _mm256_cvtph_ps          // f16→f32 (Haswell F16C)
  _mm512_cvtpbh_ps         // bf16→f32 (Genoa)
  _mm256_cvtepi8_epi32     // i8→i32 扩展

统一接口 = 在所有后端共享同一套逻辑，硬件加速自然嵌入
```

---

**返回导航**: [课程导航与学习路线](./00_课程导航与学习路线.md)
